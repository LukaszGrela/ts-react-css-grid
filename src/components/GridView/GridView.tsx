import React from 'react';

import './styles/index.scss';

const GridView: React.FC = (): JSX.Element => (
  <div className="GridView">Main Content - the grid sandbox - GridView</div>
);

export default GridView;
